version: "2"
sql:
  - schema: "migrations"
    queries: "sqlc/queries"
    engine: "postgresql"
    gen:
      go:
        package: "sqlc"
        out: "sqlc"
        emit_json_tags: true
        overrides:
          - db_type: "pg_catalog.numeric"
            go_type: "float64"